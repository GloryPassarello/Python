def lookup(d, key):
    found = False
    for child in d:
        if found : return child.text
        if child.tag == 'key' and child.text == key :
            found = True
    return None



found here is a flag that has boolean values: 
0 or 1
TRUE or FALSE

found = False					At the beginning it is set as False

if found: return child.text			if found is = False then will return child.text

if child.tag == 'key' and child.text == key :	if this condition is respected, found will set as True
    found = True
