git branch						create, delete or list branches
git branch <name>				create a new branch with the name you gives
git checkout <name>				switch to an existing branch. point the HEAD to the new branch
git log							shows commit logs 
usefull run this script 		git log --oneline --decorate
								git log --oneline --decorate --graph --all		it allows you to see all the made commits with branches

____________________________________________________________________________________________________
#making the push after git checkout
 git push --set-upstream origin <test_branch>	