Kernel created for julia python and R

jupyter + python required ipython

jupyther + R required dev toolbox

	
	install.packages('devtools')
	devtools::install_github('IRkernel/IRkernel')	
	IRkernel::installspec(name = 'ir33', displayname = 'R 3.3')

to start jupter 

	jupyter notebook  

if you want to run a new kernel

* go to new
* choose the language you want to run

If you also have python-matplotlib installed, the first line of code to enable plotting in the current Notebook is 
%matplotlib inline
